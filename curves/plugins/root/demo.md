There is (at the left) one main plugin. It is a table of color (left) and close btn right
Clicking on a color opens (if not already exists) an iframe plugin for config of that color. Otherwise the plugin for it flashes.
You can input a color.
If the div is closed, the iframe closes automagically.


Todo
===========
- System Files
- get rid of versions w/ plugins
- check that if a local plugin folder with index file has a subfodler with index that this is not going to be a plugin

