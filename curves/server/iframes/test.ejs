<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || "EJS Random Background" %></title>
    <style>
      body {
        margin: 0;
        padding: 0;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        transition: background 0.5s ease-in-out;
      }

      h1 {
        font-size: 2em;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
      }
    </style>
  </head>
  <body>
    <h1><%= title || "Welcome to the EJS Page" %></h1>

    <script>
      // Pick a random background color at render
      const colors = [
        "#ff6b6b",
        "#6bc5ff",
        "#6bff95",
        "#f0db4f",
        "#936bff",
        "#ffb86b",
      ];
      document.body.style.backgroundColor =
        colors[Math.floor(Math.random() * colors.length)];
    </script>
    <script src="/plugins/test.js" defer></script>
  </body>
</html>
